{
  "name": "automated-technical-writer-root",
  "version": "1.0.0",
  "description": "Root workspace proxy scripts",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
